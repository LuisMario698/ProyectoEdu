name: proyectoeducativo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  responsive_builder: ^0.7.0
  # Reemplazando file_picker por image_picker que ya está en las dependencias
  # file_picker: ^6.1.1
  flutter_plugin_android_lifecycle: ^2.0.17
  image_picker: ^1.0.4
  table_calendar: ^3.0.8
  sqflite: ^2.3.0
  path: ^1.8.3
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true



